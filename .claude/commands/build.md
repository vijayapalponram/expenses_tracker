Build the project using Gradle.

Run: `./gradlew build`

Report:
- Whether the build succeeded or failed
- Number of tests that ran and passed/failed (from build output)
- Any compilation errors clearly highlighted
- The path to the generated JAR if successful
