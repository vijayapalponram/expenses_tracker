Start the Spring Boot application.

Run: `./gradlew bootRun`

After the app starts:
- Confirm it is listening on port 5000
- Print the base URL: http://localhost:5000
- Print the H2 console URL: http://localhost:5000/h2-console
- List the available API endpoints from the controllers
